name: mac-control
version: 1.0.0
description: "/mac slash command: take screenshots, run commands, open URLs, and run AppleScript on MACinCloud VMs stored in the vault — no credentials visible to agents."
author: "local"
hooks: []
